/// Flat screen router — mirrors GrayManApp.swift `Screen` enum.
enum AppScreen {
  onboarding,
  phoneAuth,
  nameEntry,
  avatarOnboarding,
  chooseRole,
  recordReel,
  explore,
  workerProfile,
  home,
  profile,
}
